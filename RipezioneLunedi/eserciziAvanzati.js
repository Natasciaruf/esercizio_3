// Scrivi una funzione applicaOperazione che prenda un array di numeri e una funzione di operazione (ad esempio somma, moltiplicazione)
//  e applichi questa operazione a tutti gli elementi dell'array. Esegui questa funzione con un'operazione di somma e moltiplicazione.

function applicaOperazione(arr){
let arr = [1, 2, 3, 4, 5, 6]
return arr.map()

}